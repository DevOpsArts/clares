apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "clares-engine.fullname" . }}-config
  labels:
    {{- include "clares-engine.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
