apiVersion: v1
kind: Secret
metadata:
  name: {{ include "clares-engine.fullname" . }}-secret
  labels:
    {{- include "clares-engine.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
